version: "2"
sql:
  - schema: "migrations"
    queries: "query"
    engine: "sqlite"
    gen:
      go:
        package: "store"
        out: "internal/store"
        emit_json_tags: true
        emit_prepared_queries: true
        emit_interface: true
        emit_exact_table_names: false
